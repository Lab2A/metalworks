{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v uvx >/dev/null 2>&1 && UV_CACHE_DIR=\"${CLAUDE_PLUGIN_DATA}/uv-cache\" uvx --from \"metalworks[mcp,arctic,reddit] @ git+https://github.com/Lab2A/metalworks@main\" metalworks version >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
